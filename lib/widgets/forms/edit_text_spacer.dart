import 'package:flutter/material.dart';
import 'package:proximity/config/values.dart';

class EditTextSpacer extends StatelessWidget {
  const EditTextSpacer({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: small_100);
  }
}
